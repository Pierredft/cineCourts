{% extends 'base.html.twig' %}

{% block title %}Modifier vidéo{% endblock %}

{% block body %}
    <div class="posBtn">
    <a href="{{ path('app_video_index') }}" class="btn btnReg2" style="width: 40%;">Retour à la liste des vidéos</a>
    </div>
    <h1 class="text-center fs-1 white">Modifier la vidéo</h1>

    {{ include('video/_form.html.twig', {'button_label': 'Modifier'}) }}


    {{ include('video/_delete_form.html.twig') }}
{% endblock %}
